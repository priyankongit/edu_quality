{
  "name": "edu_quality",
  "version": "1.0.1",
  "description": "Edu Quality",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd walsh && yarn install && cd ../ui && yarn install && cd ../cmap_mgr && yarn install",
    "dev": "cd walsh && yarn dev --host",
    "build": "cd walsh && yarn build && cd ../ui && yarn build && cd ../cmap_mgr && yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "formiojs": "^4.13.1"
  }
}
